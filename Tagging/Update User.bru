meta {
  name: Update User
  type: http
  seq: 3
  tags: [
    regression
  ]
}

put {
  url: https://reqres.in/api/users/2
  body: json
  auth: inherit
}

body:json {
  {
      "name": "morpheus",
      "job": "zion resident"
  }
}

assert {
  res.status: eq 200
}

tests {
  test("status code test", function() {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
