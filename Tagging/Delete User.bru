meta {
  name: Delete User
  type: http
  seq: 4
  tags: [
    skip
  ]
}

delete {
  url: https://reqres.in/api/users/2
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("status code test", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

settings {
  encodeUrl: true
}
